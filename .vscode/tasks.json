{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build",
      "type": "shell",
      "command": "cargo build --bin squirrel && codesign -s - -f --entitlements entitlements.plist target/debug/squirrel",
      "args": [],
      "group": "build",
      "problemMatcher": ["$rustc"]
    }
  ]
}