{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build",
      "type": "shell",
      "command": "cargo build --bin sq && codesign -s - -f --entitlements entitlements.plist target/debug/sq",
      "args": [],
      "group": "build",
      "problemMatcher": ["$rustc"]
    }
  ]
}